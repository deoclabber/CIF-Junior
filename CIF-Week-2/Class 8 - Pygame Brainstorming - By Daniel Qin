import pygame
pygame.init()
clock = pygame.time.Clock()
while True:
    clock.tick(30)
    #initiate screen 
    #create box sprite
    #set box sprite to center (0, 0)
    #change x by 2 each frame for 30 frames to move right
    #the x should be 60 while the y is 0
    pygame.display.update()
